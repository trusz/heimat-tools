name: "Package: Staffing"
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "packages/staffing/**"
jobs:
  build:
    uses: "./.github/workflows/build.yml"
    with:
      package-name: recruiting
    secrets:
      BUILD_TOKEN: ${{ secrets.GITHUB_TOKEN }}